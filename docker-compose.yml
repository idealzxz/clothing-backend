name: clothing-backend

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: clothing-backend:latest
    container_name: clothing-backend
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - docker.env
    volumes:
      - sqlite_data:/app/data

volumes:
  sqlite_data:
